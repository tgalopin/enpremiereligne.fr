{% extends 'page_base.html.twig' %}

{% block metas %}
    <meta name="robots" content="noindex">
{% endblock %}

{% block content %}
    <div class="process-box">
        <div class="bg-light p-3 p-lg-4">
            <h3 class="text-center font-weight-bold mb-4">
                Ma proposition d'aide
            </h3>

            <div class="process-details">
                <p>
                    Voulez-vous vraiment supprimer votre proposition et toutes les données
                    associées ? Cette opération est irréversible.
                </p>

                <div class="text-center mt-4">
                    <a href="{{ path('process_helper_delete_do', {'uuid': helper.uuid, 'token': csrf_token('helper_delete')}) }}"
                       class="btn btn-danger">
                        Oui, supprimer
                    </a>
                    <a href="{{ path('process_helper_view', {'uuid': helper.uuid}) }}"
                       class="btn btn-link">
                        Non, retour
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
